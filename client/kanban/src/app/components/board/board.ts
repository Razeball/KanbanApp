import { Component, Input } from '@angular/core';
import { List } from '../list/list';

@Component({
  selector: 'app-board',
  imports: [List],
  templateUrl: './board.html',
  styleUrl: './board.css'
})
export class Board {

}
